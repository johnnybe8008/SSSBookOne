import { Text, View } from "react-native";

// Archived route kept intentionally; hidden placeholder avoids Expo Router warnings.
export default function AdminClientOrgOldArchived() {
	return (
		<View style={{ display: "none" }}>
			<Text>Archived screen</Text>
		</View>
	);
}