import { Text, View } from "react-native";

// Backup route kept intentionally; hidden placeholder avoids Expo Router warnings.
export default function AdminOrganizationsBackupArchived() {
	return (
		<View style={{ display: "none" }}>
			<Text>Backup screen</Text>
		</View>
	);
}

